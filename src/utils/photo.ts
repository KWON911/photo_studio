import type{Frame,LayoutPreset,Photo,PhotoFilter,PhotoTransform}from'../types/photo';import{cropRect,defaultTransform}from'./crop';import{layoutById}from'../layouts/presets';
export async function capture(v:HTMLVideoElement,mirrored:boolean){const c=document.createElement('canvas');c.width=v.videoWidth||1280;c.height=v.videoHeight||720;const x=c.getContext('2d')!;if(mirrored){x.translate(c.width,0);x.scale(-1,1)}x.drawImage(v,0,0,c.width,c.height);return new Promise<Blob>((ok,no)=>c.toBlob(b=>b?ok(b):no(Error('저장 오류')),'image/jpeg',.92))}const load=(u:string)=>new Promise<HTMLImageElement>((ok,no)=>{const i=new Image();i.onload=()=>ok(i);i.onerror=no;i.src=u});export async function compose(photos:Photo[],f:Frame,filter:PhotoFilter,transforms:Record<string,PhotoTransform>,showDate:boolean,message='',date=new Date(),layout:LayoutPreset=layoutById('classic')){const c=document.createElement('canvas');c.width=layout.outputWidth;c.height=layout.outputHeight;const x=c.getContext('2d')!;x.fillStyle=f.background;x.fillRect(0,0,c.width,c.height);x.filter=filter.cssFilter;for(const[i,p]of photos.entries()){const s=layout.slots[i],w=s.width*c.width,h=s.height*c.height,img=await load(p.url),r=cropRect(img.width,img.height,w,h,transforms[p.id]??defaultTransform);x.drawImage(img,r.left,r.top,r.width,r.height,s.x*c.width,s.y*c.height,w,h)}x.filter='none';x.fillStyle=f.color;x.font='500 34px system-ui';x.textAlign='center';const d=`${date.getFullYear()}.${String(date.getMonth()+1).padStart(2,'0')}.${String(date.getDate()).padStart(2,'0')}`,lines=[message,[f.label,showDate?d:''].filter(Boolean).join(' · ')].filter(Boolean);lines.forEach((l,i)=>x.fillText(l,c.width/2,c.height-68-(lines.length-i-1)*46));return new Promise<Blob>((ok,no)=>c.toBlob(b=>b?ok(b):no(Error('합성 오류')),'image/png'))}export const name=()=>{const d=new Date(),z=(n:number)=>String(n).padStart(2,'0');return `photo-studio_${d.getFullYear()}${z(d.getMonth()+1)}${z(d.getDate())}_${z(d.getHours())}${z(d.getMinutes())}${z(d.getSeconds())}.png`};
