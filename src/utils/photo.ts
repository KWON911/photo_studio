import type {Frame,Photo,PhotoFilter,PhotoTransform} from '../types/photo'; import {cropRect,defaultTransform} from './crop'; export async function capture(video:HTMLVideoElement,mirrored:boolean){const c=document.createElement('canvas');c.width=video.videoWidth||1280;c.height=video.videoHeight||720;const x=c.getContext('2d');if(!x)throw Error('Canvas 오류');if(mirrored){x.translate(c.width,0);x.scale(-1,1)}x.drawImage(video,0,0,c.width,c.height);return new Promise<Blob>((ok,no)=>c.toBlob(b=>b?ok(b):no(Error('저장 오류')),'image/jpeg',.92))} const image=(url:string)=>new Promise<HTMLImageElement>((ok,no)=>{const i=new Image();i.onload=()=>ok(i);i.onerror=no;i.src=url}); export async function compose(photos:Photo[],f:Frame,filter:PhotoFilter,transforms:Record<string,PhotoTransform>,showDate:boolean,date=new Date()){const w=1440,p=f.padding*3,g=f.gap*3,pw=w-p*2,ph=Math.round(pw*.75),c=document.createElement('canvas');c.width=w;c.height=p*2+ph*4+g*3+f.footerHeight;const x=c.getContext('2d');if(!x)throw Error('Canvas 오류');x.fillStyle=f.background;x.fillRect(0,0,c.width,c.height);x.filter=filter.cssFilter;for(const[n,photo]of photos.entries()){const i=await image(photo.url),r=cropRect(i.width,i.height,pw,ph,transforms[photo.id]??defaultTransform);x.drawImage(i,r.left,r.top,r.width,r.height,p,p+n*(ph+g),pw,ph)}x.filter='none';x.fillStyle=f.color;x.font='500 34px system-ui';x.textAlign='center';const dateText=`${date.getFullYear()}.${String(date.getMonth()+1).padStart(2,'0')}.${String(date.getDate()).padStart(2,'0')}`;const text=[f.label,showDate&&f.showDate!==false?dateText:''].filter(Boolean).join('  ·  ');if(text)x.fillText(text,w/2,c.height-68);return new Promise<Blob>((ok,no)=>c.toBlob(b=>b?ok(b):no(Error('합성 오류')),'image/png'))} export const name=()=>{const d=new Date(),z=(n:number)=>String(n).padStart(2,'0');return `gamsung-sajingwan_${d.getFullYear()}${z(d.getMonth()+1)}${z(d.getDate())}_${z(d.getHours())}${z(d.getMinutes())}${z(d.getSeconds())}.png`};
