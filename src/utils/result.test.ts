import{describe,expect,it}from'vitest';import{createPhotoFile,messageLimit,truncateMessage}from'./result';describe('result utilities',()=>{it('limits a message to 30 characters',()=>expect(truncateMessage('a'.repeat(40))).toHaveLength(messageLimit));it('creates a JPEG File for Web Share',()=>expect(createPhotoFile(new Blob(['photo']),'photo.jpg').type).toBe('image/jpeg'))});
