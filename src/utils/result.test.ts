import{afterEach,describe,expect,it,vi}from'vitest';import{createPhotoFile,isIOSDevice,messageLimit,truncateMessage}from'./result';describe('result utilities',()=>{afterEach(()=>vi.unstubAllGlobals());it('limits a message to 30 characters',()=>expect(truncateMessage('a'.repeat(40))).toHaveLength(messageLimit));it('creates a JPEG File for Web Share',()=>expect(createPhotoFile(new Blob(['photo']),'photo.jpg').type).toBe('image/jpeg'));it('recognizes iPhones as iOS devices',()=>{vi.stubGlobal('navigator',{userAgent:'Mozilla/5.0 (iPhone; CPU iPhone OS 18_0 like Mac OS X)',platform:'iPhone',maxTouchPoints:1});expect(isIOSDevice()).toBe(true)});it('recognizes iPadOS devices that identify as Macs',()=>{vi.stubGlobal('navigator',{userAgent:'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15)',platform:'MacIntel',maxTouchPoints:5});expect(isIOSDevice()).toBe(true)});});
