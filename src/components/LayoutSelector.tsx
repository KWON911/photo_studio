import type{LayoutId}from'../types/photo';import{layouts}from'../layouts/presets';export function LayoutSelector({value,onChange}:{value:LayoutId;onChange:(id:LayoutId)=>void}){return <nav className="layout-list">{layouts.map(l=><button aria-pressed={value===l.id} className={value===l.id?'active':''} onClick={()=>onChange(l.id)}><i style={{aspectRatio:String(l.previewAspectRatio)}}>{l.slots.map(s=><b style={{left:`${s.x*100}%`,top:`${s.y*100}%`,width:`${s.width*100}%`,height:`${s.height*100}%`}}/>)}</i><span>{l.name}</span></button>)}</nav>}
