import type{LayoutId}from'../types/photo';
import{getLayoutAspectRatio,layouts}from'../layouts/presets';
export function LayoutSelector({value,onChange}:{value:LayoutId;onChange:(id:LayoutId)=>void}){return <nav className="layout-list">{layouts.map(layout=><button key={layout.id} aria-pressed={value===layout.id} className={value===layout.id?'active':''} onClick={()=>onChange(layout.id)}><i style={{aspectRatio:String(getLayoutAspectRatio(layout))}}>{layout.slots.map((slot,index)=><b key={index} style={{left:`${slot.x*100}%`,top:`${slot.y*100}%`,width:`${slot.width*100}%`,height:`${slot.height*100}%`}}/>)}</i><span>{layout.name}</span></button>)}</nav>}
