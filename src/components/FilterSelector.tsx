import type{Photo,PhotoFilter}from'../types/photo';import type{SkinRetouchLevel}from'../filters/skin-retouch';import{filters}from'../filters/presets';import{FilteredPreviewImage}from'../filters/preview';export function FilterSelector({photo,value,onChange,skinRetouch='natural'}:{photo:Photo;value:PhotoFilter['id'];onChange:(id:PhotoFilter['id'])=>void;skinRetouch?:SkinRetouchLevel}){return <div className="filter-list">{filters.map(f=><button aria-pressed={value===f.id} className={value===f.id?'active':''} onClick={()=>onChange(f.id)}><FilteredPreviewImage source={photo.url} filter={f} maxEdge={160} skinRetouch={skinRetouch} alt=""/><span>{f.name}</span></button>)}</div>}
