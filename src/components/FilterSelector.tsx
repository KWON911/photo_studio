import type{Photo,PhotoFilter}from'../types/photo';import{filters}from'../filters/presets';export function FilterSelector({photo,value,onChange}:{photo:Photo;value:PhotoFilter['id'];onChange:(id:PhotoFilter['id'])=>void}){return <div className="filter-list">{filters.map(f=><button aria-pressed={value===f.id} className={value===f.id?'active':''} onClick={()=>onChange(f.id)}><img src={photo.url} style={{filter:f.cssFilter}} alt=""/><span>{f.name}</span></button>)}</div>}
