export type Screen = 'landing' | 'setup' | 'capture' | 'select' | 'edit' | 'result';
export type CaptureMode = 'auto' | 'manual';
export type Settings = { interval: 3 | 5 | 10; facingMode: 'user' | 'environment'; captureMode: CaptureMode };
export type Photo = { id: string; blob: Blob; url: string };
export type PhotoTransform = { scale: number; offsetX: number; offsetY: number };
export type FilterId = 'original' | 'mono' | 'soft-portrait' | 'clean-bright' | 'warm-film' | 'matte-film';
export type FilterAdjustments = { exposure: number; contrast: number; saturation: number; temperature: number; tint: number; blackLift: number; shadows: number; highlights: number; vignette: number; grain: number };
export type PhotoFilter = { id: FilterId; name: string; adjustments: FilterAdjustments };
export type FrameId = 'white' | 'black' | 'cream' | 'charcoal' | 'dusty-pink' | 'sage' | 'navy' | 'film' | 'studio' | 'negative' | 'archive';
export type FrameDecoration = 'plain' | 'studio' | 'film' | 'archive';
export type Frame = { id: FrameId; name: string; background: string; color: string; gap: number; padding: number; footerHeight: number; label?: string; showDate?: boolean; decoration?: FrameDecoration };
export type LayoutId = 'classic' | 'grid' | 'wide';
export type LayoutSlot = { x: number; y: number; width: number; height: number };
export type LayoutPreset = { id: LayoutId; name: string; outputWidth: number; outputHeight: number; footerY: number; footerHeight: number; previewAspectRatio: number; slots: LayoutSlot[] };
export type TypographyId = 'modern' | 'serif' | 'handwriting' | 'soft';
export type TypographyPreset = { id: TypographyId; name: string; fontName: string; fontFamily: string; fallbackFontFamily: string; messageWeight: number; dateWeight: number; letterSpacing: number };
export type TextAlignment = 'left' | 'center' | 'right';
export type TextSize = 'small' | 'medium' | 'large';
export const defaultTextSize: TextSize = 'medium';
