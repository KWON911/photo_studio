import{useEffect,useMemo,useState}from'react';import type{FilterId,FrameId,LayoutId,Photo,PhotoTransform,Screen,Settings}from'./types/photo';import{frames,frameById}from'./frames/presets';import{filterById}from'./filters/presets';import{layoutById}from'./layouts/presets';import{capture,compose,name}from'./utils/photo';import{useCamera}from'./hooks/useCamera';import{PhotoStrip}from'./components/PhotoStrip';import{PhotoEditor}from'./components/PhotoEditor';import{FilterSelector}from'./components/FilterSelector';import{LayoutSelector}from'./components/LayoutSelector';import{defaultTransform}from'./utils/crop';import{canSharePhoto,createPhotoFile,downloadBlob,messageLimit,truncateMessage}from'./utils/result';import'./edit.css';
import './layout.css';import './mobile-polish.css';
const wait=(n:number)=>new Promise(r=>setTimeout(r,n));
function Landing({go}:{go:()=>void}){return <main className="landing"><i>GAMSUNG<br/>PHOTO STUDIO</i><section><h1>감성사진관</h1><p>오래 남기고 싶은 순간을<br/>네 장의 사진으로.</p></section><button className="primary" onClick={go}>시작하기</button></main>}
function Setup({s,set,go}:{s:Settings;set:(x:Partial<Settings>)=>void;go:()=>void}){return <main className="screen setup"><header><em>촬영 준비</em><h1>잠시만 설정할게요.</h1></header><article><span>촬영 간격</span><nav>{([3,5,10]as const).map(n=><button className={s.interval===n?'on':''} onClick={()=>set({interval:n})}>{n}초</button>)}</nav></article><article><span>카메라</span><nav><button className={s.facingMode==='user'?'on':''} onClick={()=>set({facingMode:'user'})}>전면</button><button className={s.facingMode==='environment'?'on':''} onClick={()=>set({facingMode:'environment'})}>후면</button></nav></article><button className="primary" onClick={go}>촬영 시작하기</button></main>}
function Capture({s,done,cancel}:{s:Settings;done:(p:Photo[])=>void;cancel:()=>void}){const{ref,ready,error,start,stop}=useCamera(s),[number,setNumber]=useState<number|null>(null),[running,setRunning]=useState(false);useEffect(()=>{void start();return stop},[start,stop]);const shoot=async()=>{setRunning(true);setNumber(-1);await wait(850);const photos:Photo[]=[];for(let n=0;n<8;n++){for(let i=n?s.interval:3;i>0;i--){setNumber(i);await wait(1000)}if(ref.current){const blob=await capture(ref.current,s.facingMode==='user');photos.push({id:crypto.randomUUID(),blob,url:URL.createObjectURL(blob)})}}stop();done(photos)};return <main className="capture"><div className="bar"><button onClick={()=>{stop();cancel()}}>×</button></div>{error?<section className="error"><p>{error}</p></section>:<><video ref={ref} autoPlay muted playsInline className={s.facingMode==='user'?'mirror':''}/>{number!==null&&<strong className="count">{number===-1?'READY':number}</strong>}<footer>{ready&&!running&&<button className="shutter" onClick={()=>void shoot()}/>}</footer></>}</main>}
function Edit({photos,frame,setFrame,filter,setFilter,layout,setLayout,transforms,setTransform,date,onDate,message,onMessage,finish}:{photos:Photo[];frame:FrameId;setFrame:(v:FrameId)=>void;filter:FilterId;setFilter:(v:FilterId)=>void;layout:LayoutId;setLayout:(v:LayoutId)=>void;transforms:Record<string,PhotoTransform>;setTransform:(id:string,v:PhotoTransform)=>void;date:boolean;onDate:(v:boolean)=>void;message:string;onMessage:(v:string)=>void;finish:()=>void}){const[tab,setTab]=useState('photo'),[active,setActive]=useState(photos[0].id),photo=photos.find(p=>p.id===active)??photos[0],preset=layoutById(layout);return <main className="screen edit"><header><em>사진 꾸미기</em><h1>오늘의 네 컷</h1></header><PhotoStrip photos={photos} frame={frameById(frame)} layout={preset} filter={filterById(filter)} transforms={transforms} active={active} onSelect={setActive}/><nav className="edit-tabs">{[['photo','사진'],['filter','필터'],['frame','프레임'],['layout','레이아웃']].map(([id,label])=><button className={tab===id?'on':''} onClick={()=>setTab(id)}>{label}</button>)}</nav>{tab==='photo'?<><PhotoEditor photo={photo} value={transforms[photo.id]??defaultTransform} onChange={v=>setTransform(photo.id,v)}/><label className="message">MESSAGE<input value={message} maxLength={messageLimit} onChange={e=>onMessage(truncateMessage(e.target.value))}/></label></>:tab==='filter'?<FilterSelector photo={photo} value={filter} onChange={setFilter}/>:tab==='frame'?<><nav className="frames">{frames.map(f=><button className={f.id===frame?'active':''} onClick={()=>setFrame(f.id)}><i style={{background:f.background}}/>{f.name}</button>)}</nav><label className="date-toggle"><input type="checkbox" checked={date} onChange={e=>onDate(e.target.checked)}/> 날짜 표시</label></>:<LayoutSelector value={layout} onChange={setLayout}/>}<button className="primary" onClick={finish}>완성하기</button></main>}
export default function App(){const[screen,setScreen]=useState<Screen>('landing'),[s,setS]=useState<Settings>({interval:5,facingMode:'user'}),[photos,setPhotos]=useState<Photo[]>([]),[ids,setIds]=useState<string[]>([]),[frame,setFrame]=useState<FrameId>('white'),[filter,setFilter]=useState<FilterId>('original'),[layout,setLayout]=useState<LayoutId>('classic'),[transforms,setTransforms]=useState<Record<string,PhotoTransform>>({}),[date,setDate]=useState(true),[message,setMessage]=useState(''),[blob,setBlob]=useState<Blob|null>(null),[url,setUrl]=useState<string|null>(null);const selected=useMemo(()=>ids.map(id=>photos.find(p=>p.id===id)).filter((p):p is Photo=>!!p),[ids,photos]);const finish=async()=>{const b=await compose(selected,frameById(frame),filterById(filter),transforms,date,message,new Date(),layoutById(layout));if(url)URL.revokeObjectURL(url);setBlob(b);setUrl(URL.createObjectURL(b));setScreen('result')};if(screen==='landing')return <Landing go={()=>setScreen('setup')}/>;if(screen==='setup')return <Setup s={s} set={x=>setS(v=>({...v,...x}))} go={()=>setScreen('capture')}/>;if(screen==='capture')return <Capture s={s} done={p=>{setPhotos(p);setTransforms(Object.fromEntries(p.map(x=>[x.id,defaultTransform])));setLayout('classic');setScreen('select')}} cancel={()=>setScreen('setup')}/>;if(screen==='select')return <main className="screen"><header><em>사진 고르기</em><h1>네 장을 골라주세요.</h1></header><div className="grid">{photos.map((p,i)=>{const n=ids.indexOf(p.id);return <button className={n>=0?'chosen':''} onClick={()=>setIds(x=>x.includes(p.id)?x.filter(id=>id!==p.id):x.length<4?[...x,p.id]:x)}><img src={p.url} alt={`${i+1}번째 사진`}/>{n>=0&&<b>{n+1}</b>}</button>})}</div><button className="primary" disabled={ids.length!==4} onClick={()=>setScreen('edit')}>꾸미러 가기</button></main>;if(screen==='edit')return <Edit photos={selected} frame={frame} setFrame={setFrame} filter={filter} setFilter={setFilter} layout={layout} setLayout={setLayout} transforms={transforms} setTransform={(id,v)=>setTransforms(x=>({...x,[id]:v}))} date={date} onDate={setDate} message={message} onMessage={setMessage} finish={()=>void finish()}/>;const file=blob?createPhotoFile(blob,name()):null;return <main className="screen result"><header><em>오늘의 순간</em><h1>완성됐어요.</h1></header>{url&&<img src={url} alt="완성된 네컷"/>}<footer className="result-actions"><button className="primary" onClick={()=>file&&canSharePhoto(file)&&void navigator.share({files:[file]})}>공유하기</button><button onClick={()=>blob&&downloadBlob(blob,name())}>사진 저장</button><button onClick={()=>setScreen('edit')}>다시 편집</button><button onClick={()=>{setLayout('classic');setIds([]);setScreen('landing')}}>새로 찍기</button></footer></main>}
